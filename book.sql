CREATE TABLE Book ( ISBN VARCHAR(20) PRIMARY KEY, Title VARCHAR(200) NOT NULL, PulicationDate DATE, AuthorID INTEGER NOT NULL, CONSTRAINT FK_Author FOREIGN KEY (AuthorID) REFERENCES Author(AuthorID) ON DELETE CASCADE );